# Binaries — block all .exe; only the release build is tracked
*.exe
!necromancerlast.exe
*.bin

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Go related
vendor/

# Grimoire runtime secrets — never commit these
grimoire_creds.json
grimoire_ai.json
